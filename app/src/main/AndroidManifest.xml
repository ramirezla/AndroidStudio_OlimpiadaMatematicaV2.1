<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ehome.olimpiadamatematicav21">

    <uses-permission android:name="android.permission.MEDIA_CONTENT_CONTROL" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:logo="@drawable/logo"
        android:theme="@style/AppTheme">
        <!--<activity android:name=".MainActivity">-->
        <activity android:name=".SplashActivity">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity"></activity>
        <activity android:name=".MostrarPrueba"></activity>
        <activity android:name=".AcercaDe"></activity>
        <activity android:name=".MostrarRespuesta"></activity>
        <activity android:name=".GuiasPDF"></activity>
    </application>

</manifest>